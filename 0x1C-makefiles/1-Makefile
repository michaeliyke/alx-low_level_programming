# Usage
# make -f 0-Makefile

# Define phony rules
.PHONY = all clear clean cls
CC := gcc
SRC := $(wildcard *.c)

# Below produces executable
all: ${SRC}
	@${CC} ${SRC} -Wall -Wextra -pedantic -Werror -std=gnu89 -g -o school

clean:
	@rm -rvf *.o school

clear: clean # Alias

cls: clean # Alias
