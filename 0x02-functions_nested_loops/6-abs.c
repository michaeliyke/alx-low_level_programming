/**
 * _abs - computes the absolute value of an integer.
 * @n: the integer whose absolute value is computed
 *
 * Return: An integer
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}