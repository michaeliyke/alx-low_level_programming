#include "main.h"
#include <stdio.h>
#include <string.h>

char *cpy(char x[], char *s);

/**
 * rev_string -  reverses a string.
 * @s: string to reverse
 *
 * Return: void
 */
void rev_string(char *s)
{
	_putchar(s);
}